/*
 * Copyright 2010-2013 icl-network.com. All rights reserved.
 * Support: http://www.icl-network.com
 *
 * JavaScript - Profile Setting
 * Version: 3.0
 */
$().ready(function() {
			
	var $inputForm = $("#inputForm");
	$inputForm.find(":submit").prop("disabled", false);
	$inputForm.validate({
		rules: {
			currentPassword: {
				required: true,
				pattern: /^[^\s&\"<>]+$/,
				minlength: 4,
				maxlength: 20
			},
			password: {
				pattern: /^[^\s&\"<>]+$/,
				minlength: 4,
				maxlength: 20
			},
			rePassword: {
				equalTo: "#password"
			},
			email: {
				required: true,
				email: true,
				remote: {
					url: "/P2P-2.0-YuXinChuangTou/admin/profile/check_email",
					data: {"previousEmail": previousEmail},
					type: "post",
					cache: false
				}
			}
		},
		messages: {
			currentPassword: {
				pattern: "非法字符"
			},
			password: {
				pattern: "非法字符"
			},
			email: {
				remote: "已存在"
			}
		}
	});
	
});
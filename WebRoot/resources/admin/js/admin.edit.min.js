/*
 * Copyright 2010-2013 icl-network.com. All rights reserved.
 * Support: http://www.icl-network.com
 *
 * JavaScript - Admin Edit
 * Version: 3.0
 */
$().ready(function() {
			
	var $inputForm = $("#inputForm");
	$inputForm.find(":submit").prop("disabled", false);
	var $validate = $inputForm.validate({
		rules: {
			password: {
				pattern: /^[^\s&\"<>]+$/,
				minlength: 4,
				maxlength: 20
			},
			rePassword: {
				equalTo: "#password"
			},
			email: {
				required: true,
				email: true,
				remote: {
					url: "/P2P-2.0-YuXinChuangTou/admin/admin/check_email",
					data: {"previousEmail": previousEmail},
					type: "post",
					cache: false
				}
			},
			roleIds: "required"
		},
		messages: {
			password: {
				pattern: "非法字符"
			},
			email: {
				remote: "已存在"
			}
		}
	});
	
	var $chosenSelect = $(".chosen-select");
	$chosenSelect.change(function() {
		$validate.element(this);
	});
	
});